# Secrets — never commit
.env
.env.*
!.env.example

# Foundry (anchor to root so it doesn't swallow src/lib/)
/out/
/cache/
/broadcast/
/lib/
/foundry.lock
/.gitmodules

# Node (frontend, once ported to React)
node_modules/
dist/
.DS_Store

# Editor
.vscode/
.idea/
